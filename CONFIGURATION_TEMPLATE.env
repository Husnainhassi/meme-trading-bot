# ================================================================
# 🤖 MEME TRADING BOT CONFIGURATION
# ================================================================
# Copy this file to .env and customize the values
# All parameters are configurable to fine-tune bot behavior

# ================================================================
# 📡 TELEGRAM CONFIGURATION 
# ================================================================
TELEGRAM_BOT_TOKEN=your_bot_token_here
SIGNAL_CHANNEL_ID=-1001234567890
PERFORMANCE_CHANNEL_ID=-1001234567891

# ================================================================
# 🗄️ DATABASE CONFIGURATION
# ================================================================
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=meme_trading_bot

# ================================================================
# 🔧 BOT OPERATION SETTINGS
# ================================================================
# Chain to monitor (solana, ethereum, bsc, etc.)
CHAIN_ID=solana

# How often to scan for new tokens (in minutes)
SCAN_INTERVAL_MINUTES=30

# Bot operation mode: "single" or "continuous"
BOT_MODE=continuous

# ================================================================
# 📊 SCORING THRESHOLDS - ⚠️ UPDATED FOR REALISTIC MEME COIN SCORING
# ================================================================

# === LEGACY SCORING THRESHOLDS ===
# Minimum boom score to consider a token (0-100) - LOWERED FOR REALISM
MIN_BOOM_SCORE=40

# Minimum liquidity required in USD
MIN_LIQUIDITY_USD=5000

# Maximum risk score allowed (0-100, lower = safer)
MAX_RISK_SCORE=70

# === COMPREHENSIVE SCORING TIERS - ⚠️ ADJUSTED FOR REALISTIC MEME COIN SCORES ===
# Most new meme coins score 25-45 points due to lack of established metrics
# These thresholds reflect actual scoring patterns observed in real tokens

TIER_A_THRESHOLD=50    # Strong Buy tier (50-100 points) - LOWERED from 80
TIER_B_THRESHOLD=30    # Monitor/Small Position tier (30-49 points) - LOWERED from 65
TIER_C_THRESHOLD=20    # Watch List tier (20-29 points) - LOWERED from 50
# Tier D is anything below 20 points (Avoid)

# ================================================================
# 🎯 SIGNAL GENERATION CRITERIA - ⚠️ UPDATED FOR REALISTIC SCORES
# ================================================================

# Should Tier A always generate signals? (true/false)
TIER_A_ALWAYS_SIGNAL=true

# Minimum score required for Tier B signals - LOWERED FOR REALISM
TIER_B_MIN_SCORE=40   # Only signal good B-tier tokens (was 70)

# Minimum score required for Tier C signals - LOWERED FOR REALISM  
TIER_C_MIN_SCORE=25   # Only signal decent C-tier tokens (was 60)

# Should Tier D never generate signals? (true/false - keep true for quality)
TIER_D_NEVER_SIGNAL=true

# ================================================================
# 💰 PRICE TARGET CONFIGURATION - CUSTOMIZE RISK/REWARD
# ================================================================

# === TIER A TARGETS (Highest Quality Tokens) ===
TIER_A_TARGET_1_PERCENT=100    # First take profit target (+100%)
TIER_A_TARGET_2_PERCENT=300    # Second take profit target (+300%)
TIER_A_STOP_LOSS_PERCENT=-15   # Stop loss (-15%)

# === TIER B TARGETS (Good Quality Tokens) ===
TIER_B_TARGET_1_PERCENT=75     # First take profit target (+75%)
TIER_B_TARGET_2_PERCENT=200    # Second take profit target (+200%)
TIER_B_STOP_LOSS_PERCENT=-20   # Stop loss (-20%)

# === TIER C TARGETS (Speculative Tokens) ===
TIER_C_TARGET_1_PERCENT=50     # First take profit target (+50%)
TIER_C_TARGET_2_PERCENT=150    # Second take profit target (+150%)
TIER_C_STOP_LOSS_PERCENT=-25   # Stop loss (-25%)

# ================================================================
# 📈 PORTFOLIO UPDATE SETTINGS
# ================================================================

# Send portfolio update every N scans (4 = every 4th scan)
PORTFOLIO_UPDATE_INTERVAL_SCANS=4

# Number of top tokens to include in portfolio updates
PORTFOLIO_TOP_TOKENS_LIMIT=10

# ================================================================
# 🔍 DATA QUALITY & VALIDATION SETTINGS
# ================================================================

# Enable detailed logging for debugging (true/false)
# Set to true only for debugging - produces verbose output
ENABLE_DETAILED_LOGGING=false

# Data quality scoring is automatic and tracks:
# - Real vs fabricated transaction counts
# - Real vs fabricated holder growth data  
# - Available social media links and metrics
# - Overall data completeness score (0-100)

# The bot now automatically:
# ✅ Extracts real social links from DexScreener
# ✅ Detects fabricated data patterns
# ✅ Uses only real API transaction counts
# ✅ Marks missing data as unavailable vs zero
# ❌ No longer generates fake holder growth from volume
# ❌ No longer generates fake transaction counts from volume

# ================================================================
# 📊 SCORING CONTEXT - UNDERSTANDING THE CHANGES
# ================================================================

# BEFORE (Old Thresholds):
# - Used fabricated data (fake transactions, fake holder growth)
# - Tier A: 80-100 (unrealistic for most meme coins)
# - Tier B: 65-79 (too high for real-world tokens)
# - Most tokens scored 18-22/100 (artificially low)

# AFTER (Current Thresholds):
# - Uses only real market data from APIs
# - Tier A: 50-100 (achievable for quality tokens)
# - Tier B: 30-49 (realistic for good tokens)
# - Most tokens score 25-45/100 (realistic range)

# RESULT:
# - Signals are now based on real data
# - Thresholds match actual scoring patterns
# - Bot generates trustworthy signals
# - Performance tracking is accurate

# Minimum boom score for tokens to appear in portfolio updates
PORTFOLIO_MIN_BOOM_SCORE=50

# ================================================================
# 🔑 API KEYS - GET FROM RESPECTIVE SERVICES
# ================================================================

# Birdeye API (get from birdeye.so) - Required for enhanced analysis
BIRDEYE_API_KEY=your_birdeye_api_key

# DexTools API (optional - get from dextools.io)
DEXTOOLS_API_KEY=your_dextools_api_key

# CoinGecko API (optional - for additional market data)
COINGECKO_API_KEY=your_coingecko_api_key

# ================================================================
# 🎛️ ADVANCED CONFIGURATION
# ================================================================

# Enable debug logging (true/false)
DEBUG=false

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Enable automatic performance tracking (true/false)
ENABLE_AUTO_TRACKING=true

# Minimum score to track performance for (saves resources)
MIN_TRACKING_SCORE=70

# Hours to track signal performance [comma-separated]
TRACK_PERFORMANCE_HOURS=1,4,12,24,48

# ================================================================
# � PAPER TRADING CONFIGURATION
# ================================================================

# Enable paper trading mode (true/false)
PAPER_TRADING=true

# Starting USD balance for paper trading
PAPER_STARTING_USD_BALANCE=10000

# USD amount per trade
PAPER_TRADE_AMOUNT_USD=25

# ================================================================
# � CONFIGURATION NOTES
# ================================================================
#
# 🎯 SIGNAL QUALITY CONTROL:
# - Higher tier thresholds = fewer but higher quality signals
# - Lower tier thresholds = more signals but potentially lower quality
# - Adjust TIER_B_MIN_SCORE and TIER_C_MIN_SCORE to control spam
#
# 💰 RISK MANAGEMENT:
# - Conservative: Lower target percentages, tighter stop losses
# - Aggressive: Higher target percentages, wider stop losses
# - Tier A should always have best risk/reward ratios
#
# 💼 PAPER TRADING:
# - Start with smaller amounts ($25-50 per trade) to test strategies
# - Increase trade amounts as you gain confidence
# - Monitor performance over 100+ trades before going live
#
# ⏰ TIMING OPTIMIZATION:
# - Bull market: Decrease SCAN_INTERVAL_MINUTES (more frequent scanning)
# - Bear market: Increase SCAN_INTERVAL_MINUTES (less noise)
# - High volatility: Tighter stop losses, quicker targets
#
# 📊 PERFORMANCE TUNING:
# - Monitor success rates and adjust thresholds accordingly
# - If too many signals: Increase tier thresholds
# - If too few signals: Decrease tier thresholds or min scores
#
# ================================================================
